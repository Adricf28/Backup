// let youtubeURL = "https://youtube.com"

// let ventana = window.open(youtubeURL)
// ventana.stop() // Para la carga de una pagina

// ventana.close()
// console.log(ventana.closed)

// print() // Imprime la pagina

// console.log(confirm("¿Estás seguro de que deseas salir del sitio web?"))

const screen = window.screen

// Distancia entre borde izquierdo de ventana y borde izquierdo de monitor
const screenLeft = window.screenLeft
// Distancia entre borde top de ventana y borde top del monitor
const screenTop = window.screenTop

const scrollX = window.scrollX
const scrollY = window.scrollY

console.log(scrollY)
